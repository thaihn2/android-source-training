package code.android.ngocthai.tabbarsample.mainapp.ui

enum class TabType (val position: Int, val title: String) {
    HOME(0, "Home Screen"),
    SEARCH(1, "Search Screen"),
    USER(2, "User Screen")
}
