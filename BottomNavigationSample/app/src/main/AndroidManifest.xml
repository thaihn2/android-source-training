<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="code.android.ngocthai.bottomnavigationsample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".custom.CustomActivity"
            android:label="@string/title_activity_custom"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".navcomponent.NavComponentActivity" />

        <activity
            android:name=".basic.BottomNavigationActivity"
            android:label="@string/title_activity_bottom_naivgation" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
